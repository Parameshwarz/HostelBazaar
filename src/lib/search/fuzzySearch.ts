// Adjust similarity thresholds
const titleThreshold = 0.4;
const descriptionThreshold = 0.3;
const tagsThreshold = 0.6;

// Refine keyword mappings
const keywordMappings = {
  // ... updated keyword mappings ...
};

// Fine-tune relevance scoring
const calculateRelevanceScore = (item, query) => {
  // ... updated relevance scoring algorithm ...
}; 